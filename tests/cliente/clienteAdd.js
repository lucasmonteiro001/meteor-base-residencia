/* eslint-env mocha */
/* eslint-disable func-names, prefer-arrow-callback */

describe('AddCliente', function () {
    beforeEach(function () {
//Faz nada por enquanto
    });

    it(' testando a adição de cliente ', function () {
        let foo = new Foo(Meteor);
        let bar = foo.bar();
        expect(bar).to.equal("woo!");
    });
});
